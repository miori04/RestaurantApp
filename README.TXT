# Indah Cafe Ordering System

A Java-based desktop application designed to streamline restaurant management and order processing. 

## Features
* **Order Tracking:** Create, process, and manage customer orders.
* **Local Storage:** Automatically saves transaction or order receipt data (utilizing the local `Orders` directory).
* *(Note: Add 1-2 more bullet points here about what your system can do, such as menu management, billing, or table tracking)*

## Prerequisites
To run this application on your machine, you must have the **Java Runtime Environment (JRE)** installed. 

## How to Run the Application
You do not need to compile the code to run the system. A pre-built executable is provided.

**Option 1: Direct Execution**
If your system is configured to run Java archives directly, simply double-click the `RestaurantApp.jar` file. 

**Option 2: Command Line**
1. Download the `RestaurantApp.jar` file. *(Ensure any accompanying `lib` folders are kept in the same relative directory if you downloaded the whole project).*
2. Open your command prompt or terminal.
3. Navigate to the folder where you saved the application.
4. Run the following command:
   ```bash
   java -jar "RestaurantApp.jar"
